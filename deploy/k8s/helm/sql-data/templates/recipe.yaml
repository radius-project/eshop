apiVersion: radius.app/v1
kind: Recipe
metadata:
  name: {{ template "sql-data.fullname" . }}
  annotations:
    radius.app/application: {{ .Values.app.name }}
    radius.app/name: {{ .Values.app.infra.sqldata }}
spec:
  type: Applications.Datastores/sqlDatabases
